import styled from 'styled-components';

export const CountryContent = styled.div`
  display: flex;
  gap: 3rem;
  padding: 2rem 0;
  flex-wrap: wrap;
  justify-content: space-between;
`;

export const ProvincesTable = styled.div`
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
`;

export const ProvinceContent = styled.div`
  flex: 1;
`;
